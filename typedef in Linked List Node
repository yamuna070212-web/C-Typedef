#include <stdio.h>

typedef struct Node
{
    int data;
    struct Node *next;
} Node;

int main()
{
    Node first = {100, NULL};

    printf("Data = %d\n", first.data);

    return 0;
}
Output
Data = 100
